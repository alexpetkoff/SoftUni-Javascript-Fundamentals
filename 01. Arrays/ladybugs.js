function ladybugs(array) {

    let fieldSize = array[0];
    let positions = array[1].split(" ");
    let finalResult = [];
    for (let i = 2; i < array.length; i++) {
        let command = array[i].split(" ");
        let initial = command[0];
        let direction = command[1];
        let final = command[2];

        

    }

    
}

ladybugs([ 3, '0 1',
'0 right 1',
'2 right 1' ]);